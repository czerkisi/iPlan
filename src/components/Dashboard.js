import React from "react";

class DashBoard extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return(
            <h2>Dashboard</h2>
        );
    }
}

export default Dashboard;